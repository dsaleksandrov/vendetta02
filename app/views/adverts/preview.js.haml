- if @article.errors.any?
  console.log('Error');
  $('#dialog-form').html('#{escape_javascript(render('preview'))}');
- else
  console.log('Created');
  $('#dialog-form').dialog('close');
  $('#dialog-form').remove();
  $('table').append('#{escape_javascript(render(@advert))}');